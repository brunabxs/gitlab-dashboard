{
    "process-manifest-template": {
        "options": {
            "data": {
                "version": "<%= pkg.version %>",
                "description": "<%= pkg.description %>"
            }
        },
        "files": {
            "app/manifest.json": ["app/manifest.template.json"]
        }
    }
}